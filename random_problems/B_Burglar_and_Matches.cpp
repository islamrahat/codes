#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main()
{
    int storage, number;
    cin >> storage >> number;
    int arr[number][2];
    for(int i=0; i<number; i++) cin >> arr[i][0] >> arr[i][1];
    
    return 0;
}